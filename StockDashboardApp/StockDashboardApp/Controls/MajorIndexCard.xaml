<UserControl x:Class="StockDashboardApp.Controls.MajorIndexCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockDashboardApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderThickness="1" BorderBrush="LightGray" CornerRadius="10">
        
        <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
            <TextBlock Text="{Binding name,FallbackValue=Name}" Grid.Row="0" FontSize="18" HorizontalAlignment="Center"/>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Price"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding price,StringFormat={}{0:c},FallbackValue=$0.00}"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Change"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding change,StringFormat={}{0:c},FallbackValue=$0.00}"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>
